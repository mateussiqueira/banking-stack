{
  "name": "@banking/devops-cicd",
  "version": "1.0.0",
  "private": true,
  "description": "Tekton-based CI/CD pipelines with GitHub integration for Banking",
  "scripts": {
    "setup": "bash scripts/setup-tekton.sh",
    "secret": "bash scripts/create-github-secret.sh",
    "apply-tasks": "kubectl apply -f tekton/tasks/",
    "apply-pipelines": "kubectl apply -f tekton/pipelines/",
    "apply-triggers": "kubectl apply -f tekton/triggers/",
    "apply-all": "npm run apply-tasks && npm run apply-pipelines && npm run apply-triggers"
  }
}
