{
  "buildCommand": "cd packages/docs && npm run build",
  "outputDirectory": "packages/docs/.vitepress/dist",
  "framework": null,
  "installCommand": "npm install --prefix packages/docs",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1.html" }
  ]
}
